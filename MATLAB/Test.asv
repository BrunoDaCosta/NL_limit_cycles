clear; close all;

x=linspace(-2*pi,2*pi,1000)';
x_test=linspace(-2*pi,2*pi,74)'
y=sin(x);


Bx = Gauss_Regression_Precompute(x);
y_pred = Gauss_Regression(Bx,y,x,x_test);


figure;hold on;
plot(x,y,'r');
scatter(x_test,y_pred,'g');